# AUTO-GENERATED — do not edit by hand.
# Source: node scripts/generate-crawler-group-workflows.mjs
# Regenerate after adding/removing/renaming a crawler (edit the crawler's
# own script + npm script, re-run: node scripts/extract-crawler-manifest.mjs
# && node scripts/generate-crawler-group-workflows.mjs).
#
# Group 13 members (26): proton, solothurner-spitaeler, tally-weijl, pigna, unispital-basel, nsn-medical, upd, lombard-odier, salina-reha, ricola, ibsa, vitrea-gesundheit, kiabi, kssg, ikea, klinik-wyssholzli, givaudan, helsinn, ksow, corner, ehnv, etat-de-fribourg, damiani, artisa, anybotics, alten
#
# Each "Crawler: ..." step below is a background:true composite shell
# script that runs that ONE crawler's own install/prep/run/housekeeping/
# commit/error-report sequence, spliced verbatim from its former dedicated
# workflow file (data/crawler-manifest.json) — commit-and-push and error-
# reporting mechanisms are UNCHANGED per crawler, just co-located in one
# job so the group holds a single GitHub Actions concurrent-job slot
# instead of one slot per crawler.

name: Crawler Group 13 (26 crawlers)
on:
  workflow_dispatch: null
concurrency:
  group: crawler-group-13
  cancel-in-progress: false
permissions:
  contents: write
  issues: write
env:
  NODE_OPTIONS: --disable-warning=DEP0040 --disable-warning=DEP0169
jobs:
  run-crawler-group-13:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
      - name: "Crawler: Update Proton Jobs (Dedicated)"
        id: crawler-proton
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-proton.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-proton.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Proton crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_PROTON_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-proton-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Proton crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="proton"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/proton.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Proton jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issue"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Solothurner Spitäler (soH) Jobs (Dedicated)"
        id: crawler-solothurner-spitaeler
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-solothurner-spitaeler.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-solothurner-spitaeler.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Solothurner Spitäler (soH) crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_SOLOTHURNER_SPITAELER_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-solothurner-spitaeler-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Solothurner Spitäler (soH) crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="solothurner-spitaeler"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/solothurner-spitaeler.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Solothurner Spitäler (soH) jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update TALLY WEiJL Jobs (Dedicated)"
        id: crawler-tally-weijl
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-tally-weijl.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-tally-weijl.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated TALLY WEiJL crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_TALLY_WEIJL_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-tally-weijl-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated TALLY WEiJL crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="tally-weijl"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/tally-weijl.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update TALLY WEiJL jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Stiftung Pigna Jobs (Dedicated)"
        id: crawler-pigna
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-pigna.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-pigna.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Pigna crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_PIGNA_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-pigna-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Pigna crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="pigna"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/pigna.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Pigna jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Universitätsspital Basel Jobs (Dedicated)"
        id: crawler-unispital-basel
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-unispital-basel.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-unispital-basel.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Universitätsspital Basel crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_UNISPITAL_BASEL_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-unispital-basel-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Universitätsspital Basel crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="unispital-basel"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/unispital-basel.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Universitätsspital Basel jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update NSN Medical Jobs (Dedicated)"
        id: crawler-nsn-medical
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-nsn-medical.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-nsn-medical.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated NSN Medical crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_NSN_MEDICAL_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-nsn-medical-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated NSN Medical crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="nsn-medical"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/nsn-medical.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update NSN Medical jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Universitäre Psychiatrische Dienste Bern (UPD) Jobs (Dedicated)"
        id: crawler-upd
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-upd.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-upd.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated UPD crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_UPD_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-upd-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated UPD crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="upd"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/upd.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update UPD jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Lombard Odier Jobs (Dedicated)"
        id: crawler-lombard-odier
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-lombard-odier.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-lombard-odier.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Lombard Odier crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_LOMBARD_ODIER_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-lombard-odier-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Lombard Odier crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="lombard-odier"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/lombard-odier.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Lombard Odier jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Salina Rehaklinik Rheinfelden Jobs (Dedicated)"
        id: crawler-salina-reha
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-salina-reha.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-salina-reha.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Install Playwright browsers"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npx playwright install --with-deps chromium
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install Playwright browsers failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Salina Rehaklinik crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_SALINA_REHA_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            export SKIP_BOILERPLATE_GUARD="1"
            node scripts/update-salina-reha-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Salina Rehaklinik crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="salina-reha"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/salina-reha.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Salina Rehaklinik Rheinfelden jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Ricola Jobs (Dedicated)"
        id: crawler-ricola
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-ricola.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-ricola.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Ricola crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_RICOLA_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-ricola-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Ricola crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="ricola"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/ricola.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Ricola jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update IBSA Jobs (Dedicated)"
        id: crawler-ibsa
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-ibsa.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-ibsa.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated IBSA crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_IBSA_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            npm run jobs:update:ibsa
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated IBSA crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="ibsa"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/ibsa.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "🧬 Auto-update IBSA jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Vitrea Gesundheit Jobs (Dedicated)"
        id: crawler-vitrea-gesundheit
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-vitrea-gesundheit.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-vitrea-gesundheit.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated VITREA_GESUNDHEIT crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_VITREA_GESUNDHEIT_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-vitrea-gesundheit-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated VITREA_GESUNDHEIT crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="vitrea-gesundheit"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/vitrea-gesundheit.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update VITREA_GESUNDHEIT jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Kiabi Suisse Jobs (Dedicated)"
        id: crawler-kiabi
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-kiabi.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-kiabi.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Kiabi Suisse crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_KIABI_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-kiabi-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Kiabi Suisse crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="kiabi"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/kiabi.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Kiabi Suisse jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Kantonsspital St. Gallen (KSSG / HOCH) Jobs (Dedicated)"
        id: crawler-kssg
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-kssg.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-kssg.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Kantonsspital St. Gallen (KSSG / HOCH) crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_KSSG_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-kssg-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Kantonsspital St. Gallen (KSSG / HOCH) crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="kssg"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/kssg.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Kantonsspital St. Gallen (KSSG / HOCH) jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update IKEA Jobs (Dedicated)"
        id: crawler-ikea
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-ikea.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-ikea.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated IKEA crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_IKEA_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-ikea-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated IKEA crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="ikea"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/ikea.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update IKEA jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issue"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Klinik Wysshölzli Jobs (Dedicated)"
        id: crawler-klinik-wyssholzli
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-klinik-wyssholzli.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-klinik-wyssholzli.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
              echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
            fi
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Klinik Wysshölzli crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_KLINIK_WYSSHOLZLI_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            export SKIP_BOILERPLATE_GUARD="1"
            node scripts/update-klinik-wyssholzli-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Klinik Wysshölzli crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="klinik-wyssholzli"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/klinik-wyssholzli.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update KLINIK_WYSSHOLZLI jobs (dedicated crawler)" data/jobs-crawler-adapters/adapters/klinik-wyssholzli.json
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Givaudan Jobs (Dedicated)"
        id: crawler-givaudan
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-givaudan.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-givaudan.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Givaudan crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_GIVAUDAN_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-givaudan-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Givaudan crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="givaudan"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/givaudan.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Givaudan jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issue"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Helsinn Healthcare SA Jobs (Dedicated)"
        id: crawler-helsinn
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-helsinn.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-helsinn.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Helsinn Healthcare SA crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_HELSINN_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            npm run jobs:update:helsinn
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Helsinn Healthcare SA crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="helsinn"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/helsinn.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💊 Auto-update Helsinn Healthcare SA jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Kantonsspital Obwalden Jobs (Dedicated)"
        id: crawler-ksow
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-ksow.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-ksow.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Install Playwright Chromium"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npx playwright install --with-deps chromium
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install Playwright Chromium failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Kantonsspital Obwalden crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_KSOW_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-ksow-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Kantonsspital Obwalden crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="ksow"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/ksow.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Kantonsspital Obwalden jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Corner Jobs (Dedicated)"
        id: crawler-corner
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-corner.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-corner.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Corner crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_CORNER_STRICT="${{ github.event.inputs.strict_localization || '0' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            npm run jobs:update:corner
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Corner crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="corner"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/corner.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "🏦 Auto-update Corner jobs (dedicated crawler)"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update eHnv Étab. Hospitaliers Nord Vaudois Jobs (Dedicated)"
        id: crawler-ehnv
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-ehnv.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-ehnv.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Install Playwright browsers"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npx playwright install --with-deps chromium
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install Playwright browsers failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated EHNV crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_EHNV_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-ehnv-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated EHNV crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="ehnv"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/ehnv.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update EHNV jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Etat de Fribourg Jobs (Dedicated)"
        id: crawler-etat-de-fribourg
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-etat-de-fribourg.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-etat-de-fribourg.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Etat de Fribourg crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_ETAT_DE_FRIBOURG_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-etat-de-fribourg-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Etat de Fribourg crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="etat-de-fribourg"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/etat-de-fribourg.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Etat de Fribourg jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Damiani Jobs (Dedicated)"
        id: crawler-damiani
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-damiani.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-damiani.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Damiani crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_DAMIANI_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            npm run jobs:update:damiani
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Damiani crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="damiani-group"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/damiani-group.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💎 Auto-update Damiani jobs (dedicated crawler)"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Artisa Group Jobs (Dedicated)"
        id: crawler-artisa
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-artisa.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-artisa.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Artisa crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_ARTISA_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            npm run jobs:update:artisa
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Artisa crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="artisa-group"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/artisa-group.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "🏗️ Auto-update Artisa Group jobs (dedicated crawler)"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update ANYbotics Jobs (Dedicated)"
        id: crawler-anybotics
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-anybotics.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-anybotics.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated ANYbotics crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_ANYBOTICS_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-anybotics-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated ANYbotics crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="anybotics"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/anybotics.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update ANYbotics jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issue"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update ALTEN Jobs (Dedicated)"
        id: crawler-alten
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-alten.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-alten.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Install Playwright browsers"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npx playwright install --with-deps chromium
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install Playwright browsers failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated ALTEN crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_ALTEN_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            xvfb-run -a npm run jobs:update:alten
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated ALTEN crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="alten-switzerland"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/alten-switzerland.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="\\${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "🧩 Auto-update ALTEN jobs (dedicated crawler)"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: Wait for all 26 crawlers in crawler-group-13
        wait-all: true
