# AUTO-GENERATED — do not edit by hand.
# Source: node scripts/generate-crawler-group-workflows.mjs
# Regenerate after adding/removing/renaming a crawler (edit the crawler's
# own script + npm script, re-run: node scripts/extract-crawler-manifest.mjs
# && node scripts/generate-crawler-group-workflows.mjs).
#
# Group 17 members (26): spital-nidwalden, senevita, michel-gruppe, sunrise, spital-davos, valiant-bank, tether, see-spital, moncucco, lups, hospice-general, vf, lwphr, kanton-zuerich, huntsman, fincons, klinik-gut, gz-dielsdorf, csc-costruzioni, csvp-poschiavo, clinique-de-la-plaine, csl-behring, constellium, balgrist, endress-hauser, ardian
#
# Each "Crawler: ..." step below is a background:true composite shell
# script that runs that ONE crawler's own install/prep/run/housekeeping/
# commit/error-report sequence, spliced verbatim from its former dedicated
# workflow file (data/crawler-manifest.json) — commit-and-push and error-
# reporting mechanisms are UNCHANGED per crawler, just co-located in one
# job so the group holds a single GitHub Actions concurrent-job slot
# instead of one slot per crawler.

name: Crawler Group 17 (26 crawlers)
on:
  workflow_dispatch: null
concurrency:
  group: crawler-group-17
  cancel-in-progress: false
permissions:
  contents: write
  issues: write
env:
  NODE_OPTIONS: --disable-warning=DEP0040 --disable-warning=DEP0169
jobs:
  run-crawler-group-17:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
      - name: "Crawler: Update Spital Nidwalden Jobs (Dedicated)"
        id: crawler-spital-nidwalden
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-spital-nidwalden.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-spital-nidwalden.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Spital Nidwalden crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_SPITAL_NIDWALDEN_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-spital-nidwalden-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Spital Nidwalden crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="spital-nidwalden"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/spital-nidwalden.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Spital Nidwalden jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Senevita Jobs (Dedicated)"
        id: crawler-senevita
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-senevita.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-senevita.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Senevita crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_SENEVITA_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-senevita-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Senevita crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="senevita"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/senevita.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Senevita jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Michel Gruppe AG Jobs (Dedicated)"
        id: crawler-michel-gruppe
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-michel-gruppe.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-michel-gruppe.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Michel Gruppe AG crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_MICHEL_GRUPPE_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-michel-gruppe-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Michel Gruppe AG crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="michel-gruppe-ag"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/michel-gruppe-ag.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Michel Gruppe AG jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Sunrise Jobs (Dedicated)"
        id: crawler-sunrise
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-sunrise.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-sunrise.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Install Playwright browsers"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npx playwright install --with-deps chromium
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install Playwright browsers failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Sunrise crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_SUNRISE_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            npm run jobs:update:sunrise
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Sunrise crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="sunrise-sede-ticino"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/sunrise-sede-ticino.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "🌅 Auto-update Sunrise jobs (dedicated crawler)"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Spital Davos Jobs (Dedicated)"
        id: crawler-spital-davos
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-spital-davos.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-spital-davos.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Spital Davos crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_SPITAL_DAVOS_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-spital-davos-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Spital Davos crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="spital-davos"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/spital-davos.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Spital Davos jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Valiant Bank Jobs (Dedicated)"
        id: crawler-valiant-bank
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-valiant-bank.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-valiant-bank.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Valiant Bank crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_VALIANT_BANK_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-valiant-bank-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Valiant Bank crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="valiant-bank"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/valiant-bank.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Valiant Bank jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Tether Operations Jobs (Dedicated)"
        id: crawler-tether
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-tether.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-tether.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Tether Operations crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_TETHER_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-tether-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Tether Operations crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="tether"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/tether.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Tether Operations jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update See-Spital Jobs (Dedicated)"
        id: crawler-see-spital
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-see-spital.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-see-spital.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated See-Spital crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_SEE_SPITAL_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-see-spital-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated See-Spital crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="see-spital"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/see-spital.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update See-Spital jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Gruppo Ospedaliero Moncucco Jobs (Dedicated)"
        id: crawler-moncucco
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-moncucco.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-moncucco.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Gruppo Ospedaliero Moncucco crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_MONCUCCO_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-moncucco-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Gruppo Ospedaliero Moncucco crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="moncucco"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/moncucco.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Gruppo Ospedaliero Moncucco jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Luzerner Psychiatrie (LUPS) Jobs (Dedicated)"
        id: crawler-lups
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-lups.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-lups.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Install Playwright Chromium"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npx playwright install --with-deps chromium
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install Playwright Chromium failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Luzerner Psychiatrie (LUPS) crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_LUPS_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-lups-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Luzerner Psychiatrie (LUPS) crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="lups"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/lups.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Luzerner Psychiatrie (LUPS) jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Hospice général Jobs (Dedicated)"
        id: crawler-hospice-general
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-hospice-general.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-hospice-general.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Hospice général crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_HOSPICE_GENERAL_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-hospice-general-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Hospice général crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="hospice-general"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/hospice-general.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Hospice général jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update VF Jobs (Dedicated)"
        id: crawler-vf
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-vf.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-vf.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated VF crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_VF_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            npm run jobs:update:vf
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated VF crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="vf"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/vf.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update VF jobs (dedicated crawler)"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update LWPHR Jobs (Dedicated)"
        id: crawler-lwphr
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-lwphr.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-lwphr.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
              echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
            fi
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated LWPHR crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_LWPHR_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            npm run jobs:update:lwphr
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated LWPHR crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="lwphr"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/lwphr.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update LWPHR jobs (dedicated crawler)" data/jobs-crawler-adapters/adapters/lwphr.json
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Kanton Zürich Jobs (Dedicated)"
        id: crawler-kanton-zuerich
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-kanton-zuerich.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-kanton-zuerich.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Kanton Zürich crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_KANTON_ZUERICH_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-kanton-zuerich-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Kanton Zürich crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="kanton-zuerich"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/kanton-zuerich.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Kanton Zürich jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Huntsman Corporation Jobs (Dedicated)"
        id: crawler-huntsman
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-huntsman.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-huntsman.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Huntsman Corporation crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_HUNTSMAN_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-huntsman-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Huntsman Corporation crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="huntsman"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/huntsman.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Huntsman Corporation jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Fincons Jobs (Dedicated)"
        id: crawler-fincons
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-fincons.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-fincons.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Fincons crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_FINCONS_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            npm run jobs:update:fincons
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Fincons crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="fincons-group"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/fincons-group.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "🏢 Auto-update Fincons jobs (dedicated crawler)"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Klinik Gut Jobs (Dedicated)"
        id: crawler-klinik-gut
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-klinik-gut.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-klinik-gut.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated KLINIK_GUT crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_KLINIK_GUT_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-klinik-gut-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated KLINIK_GUT crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="klinik-gut"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/klinik-gut.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update KLINIK_GUT jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Gesundheitszentrum Dielsdorf Jobs (Dedicated)"
        id: crawler-gz-dielsdorf
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-gz-dielsdorf.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-gz-dielsdorf.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated GZ_DIELSDORF crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_GZ_DIELSDORF_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-gz-dielsdorf-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated GZ_DIELSDORF crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="gz-dielsdorf"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/gz-dielsdorf.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update GZ_DIELSDORF jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update CSC Costruzioni Jobs (Dedicated)"
        id: crawler-csc-costruzioni
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-csc-costruzioni.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-csc-costruzioni.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated CSC Costruzioni crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_CSC_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            npm run jobs:update:csc-costruzioni
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated CSC Costruzioni crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="csc-costruzioni"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/csc-costruzioni.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "🏗️ Auto-update CSC Costruzioni jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update CSVP Centro Sanitario Valposchiavo Jobs (Dedicated)"
        id: crawler-csvp-poschiavo
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-csvp-poschiavo.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-csvp-poschiavo.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated CSVP_POSCHIAVO crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_CSVP_POSCHIAVO_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-csvp-poschiavo-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated CSVP_POSCHIAVO crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="csvp-poschiavo"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/csvp-poschiavo.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update CSVP_POSCHIAVO jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Clinique de la Plaine Jobs (Dedicated)"
        id: crawler-clinique-de-la-plaine
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-clinique-de-la-plaine.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-clinique-de-la-plaine.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Clinique de la Plaine crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_CLINIQUE_DE_LA_PLAINE_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-clinique-de-la-plaine-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Clinique de la Plaine crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="clinique-de-la-plaine"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/clinique-de-la-plaine.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Clinique de la Plaine jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update CSL Behring Jobs (Dedicated)"
        id: crawler-csl-behring
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-csl-behring.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-csl-behring.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated CSL_BEHRING crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_CSL_BEHRING_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-csl-behring-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated CSL_BEHRING crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="csl-behring"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/csl-behring.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update CSL_BEHRING jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Constellium Valais Jobs (Dedicated)"
        id: crawler-constellium
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-constellium.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-constellium.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Constellium Valais crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_CONSTELLIUM_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-constellium-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Constellium Valais crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="constellium"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/constellium.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Constellium Valais jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Universitätsklinik Balgrist (Prospective) Jobs (Dedicated)"
        id: crawler-balgrist
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-balgrist.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-balgrist.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated BALGRIST crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_BALGRIST_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-balgrist-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated BALGRIST crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="balgrist"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/balgrist.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update BALGRIST jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Endress+Hauser Jobs (Dedicated)"
        id: crawler-endress-hauser
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-endress-hauser.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-endress-hauser.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Endress+Hauser crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_ENDRESS_HAUSER_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-endress-hauser-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Endress+Hauser crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="endress-hauser"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/endress-hauser.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Endress+Hauser jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issue"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Ardian Jobs (Dedicated)"
        id: crawler-ardian
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-ardian.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-ardian.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Ardian crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_ARDIAN_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-ardian-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Ardian crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="ardian"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/ardian.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Ardian jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issue"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: Wait for all 26 crawlers in crawler-group-17
        wait-all: true
