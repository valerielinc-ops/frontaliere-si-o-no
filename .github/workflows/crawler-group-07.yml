# AUTO-GENERATED — do not edit by hand.
# Source: node scripts/generate-crawler-group-workflows.mjs
# Regenerate after adding/removing/renaming a crawler (edit the crawler's
# own script + npm script, re-run: node scripts/extract-crawler-manifest.mjs
# && node scripts/generate-crawler-group-workflows.mjs).
#
# Group 07 members (26): richemont, spital-thusis, stiftung-diaconis, jysk, ukbb, soh-solothurner-spitaeler, spital-uster, livingcircle, klinik-lengg, klinik-siloah, rss-surselva, klinik-adelheid, spitex-basel, ergolz-klinik, gkb, pizzarotti, hoval, gemeinde-st-moritz, cds-savognin, groupe-mutuel, holmes-place, citta-di-bellinzona, buehler, belimo, clinique-la-source, afry
#
# Each "Crawler: ..." step below is a background:true composite shell
# script that runs that ONE crawler's own install/prep/run/housekeeping/
# commit/error-report sequence, spliced verbatim from its former dedicated
# workflow file (data/crawler-manifest.json) — commit-and-push and error-
# reporting mechanisms are UNCHANGED per crawler, just co-located in one
# job so the group holds a single GitHub Actions concurrent-job slot
# instead of one slot per crawler.

name: Crawler Group 07 (26 crawlers)
on:
  workflow_dispatch: null
concurrency:
  group: crawler-group-07
  cancel-in-progress: false
permissions:
  contents: write
  issues: write
env:
  NODE_OPTIONS: --disable-warning=DEP0040 --disable-warning=DEP0169
jobs:
  run-crawler-group-07:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
      - name: "Crawler: Update Richemont Jobs (Dedicated)"
        id: crawler-richemont
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-richemont.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-richemont.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Install Playwright Chromium"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npx playwright install --with-deps chromium
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install Playwright Chromium failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Richemont crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_RICHEMONT_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-richemont-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Richemont crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="richemont"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/richemont.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Richemont jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Spital Thusis Jobs (Dedicated)"
        id: crawler-spital-thusis
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-spital-thusis.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-spital-thusis.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Spital Thusis crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_SPITAL_THUSIS_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-spital-thusis-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Spital Thusis crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="spital-thusis"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/spital-thusis.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Spital Thusis jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Stiftung Diaconis Jobs (Dedicated)"
        id: crawler-stiftung-diaconis
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-stiftung-diaconis.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-stiftung-diaconis.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Stiftung Diaconis crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_STIFTUNG_DIACONIS_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-stiftung-diaconis-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Stiftung Diaconis crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="stiftung-diaconis"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/stiftung-diaconis.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Stiftung Diaconis jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update JYSK Jobs (Dedicated)"
        id: crawler-jysk
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-jysk.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-jysk.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated JYSK crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_JYSK_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            npm run jobs:update:jysk
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated JYSK crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="jysk"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/jysk.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "🛋️ Auto-update JYSK jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update UKBB Jobs (Dedicated)"
        id: crawler-ukbb
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-ukbb.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-ukbb.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated UKBB crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_UKBB_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-ukbb-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated UKBB crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="ukbb"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/ukbb.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update UKBB jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Solothurner Spitäler (soH) Jobs (Dedicated)"
        id: crawler-soh-solothurner-spitaeler
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-soh-solothurner-spitaeler.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-soh-solothurner-spitaeler.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated SOH crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_SOH_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-soh-solothurner-spitaeler-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated SOH crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="soh-solothurner-spitaeler"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/soh-solothurner-spitaeler.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update SOH jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Spital Uster Jobs (Dedicated)"
        id: crawler-spital-uster
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-spital-uster.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-spital-uster.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Spital Uster crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_SPITAL_USTER_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-spital-uster-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Spital Uster crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="spital-uster"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/spital-uster.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Spital Uster jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update The Living Circle Jobs (Dedicated)"
        id: crawler-livingcircle
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-livingcircle.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-livingcircle.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Setup Firebase service account"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Setup Firebase service account failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated The Living Circle crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_LIVINGCIRCLE_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            npm run jobs:update:livingcircle
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated The Living Circle crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Scoped housekeeping"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="the-living-circle"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Scoped housekeeping failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Commit updated data"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update The Living Circle jobs (dedicated crawler)" data/jobs-crawler-adapters/adapters/the-living-circle.json
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit updated data failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Klinik Lengg (Prospective) Jobs (Dedicated)"
        id: crawler-klinik-lengg
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-klinik-lengg.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-klinik-lengg.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated KLINIK_LENGG crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_KLINIK_LENGG_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-klinik-lengg-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated KLINIK_LENGG crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="klinik-lengg"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/klinik-lengg.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update KLINIK_LENGG jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Klinik Siloah Jobs (Dedicated)"
        id: crawler-klinik-siloah
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-klinik-siloah.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-klinik-siloah.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
              echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
            fi
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated KLINIK_SILOAH crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_KLINIK_SILOAH_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            export SKIP_BOILERPLATE_GUARD="1"
            node scripts/update-klinik-siloah-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated KLINIK_SILOAH crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="klinik-siloah"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/klinik-siloah.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update KLINIK_SILOAH jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Regionalspital Surselva Jobs (Dedicated)"
        id: crawler-rss-surselva
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-rss-surselva.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-rss-surselva.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Regionalspital Surselva crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_RSS_SURSELVA_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-rss-surselva-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Regionalspital Surselva crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="rss-surselva"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/rss-surselva.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Regionalspital Surselva jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Klinik Adelheid (bespoke CMS) Jobs (Dedicated)"
        id: crawler-klinik-adelheid
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-klinik-adelheid.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-klinik-adelheid.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated KLINIK-ADELHEID crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_KLINIK_ADELHEID_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-klinik-adelheid-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated KLINIK-ADELHEID crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="klinik-adelheid"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/klinik-adelheid.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update KLINIK-ADELHEID jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update SPITEX BASEL (Prospective) Jobs (Dedicated)"
        id: crawler-spitex-basel
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-spitex-basel.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-spitex-basel.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated SPITEX BASEL crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_SPITEX_BASEL_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-spitex-basel-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated SPITEX BASEL crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="spitex-basel"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/spitex-basel.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update SPITEX BASEL jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Ergolz Klinik Jobs (Dedicated)"
        id: crawler-ergolz-klinik
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-ergolz-klinik.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-ergolz-klinik.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Ergolz Klinik crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_ERGOLZ_KLINIK_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-ergolz-klinik-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Ergolz Klinik crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="ergolz-klinik"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/ergolz-klinik.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Ergolz Klinik jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Graubündner Kantonalbank Jobs (Dedicated)"
        id: crawler-gkb
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-gkb.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-gkb.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Graubündner Kantonalbank crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_GKB_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-gkb-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Graubündner Kantonalbank crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="gkb"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/gkb.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Graubündner Kantonalbank jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Pizzarotti Jobs (Dedicated)"
        id: crawler-pizzarotti
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-pizzarotti.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-pizzarotti.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Pizzarotti crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_PIZZAROTTI_STRICT="${{ github.event.inputs.strict_localization || '0' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            npm run jobs:update:pizzarotti
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Pizzarotti crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="impresa-pizzarotti"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/impresa-pizzarotti.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "🏗️ Auto-update Impresa Pizzarotti jobs (dedicated crawler)"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Hoval jobs"
        id: crawler-hoval
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-hoval.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-hoval.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci --ignore-scripts
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run Hoval dedicated crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-hoval-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run Hoval dedicated crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="hoval"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/hoval.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "🏗️ Auto-update Hoval jobs (dedicated crawler)"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Gemeinde St. Moritz Jobs (Dedicated)"
        id: crawler-gemeinde-st-moritz
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-gemeinde-st-moritz.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-gemeinde-st-moritz.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Gemeinde St. Moritz crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_GEMEINDE_ST_MORITZ_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-gemeinde-st-moritz-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Gemeinde St. Moritz crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="gemeinde-st-moritz"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/gemeinde-st-moritz.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Gemeinde St. Moritz jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update CDS Savognin Jobs (Dedicated)"
        id: crawler-cds-savognin
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-cds-savognin.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-cds-savognin.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated CDS SAVOGNIN crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_CDS_SAVOGNIN_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-cds-savognin-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated CDS SAVOGNIN crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="cds-savognin"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/cds-savognin.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update CDS SAVOGNIN jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Groupe Mutuel Jobs (Dedicated)"
        id: crawler-groupe-mutuel
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-groupe-mutuel.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-groupe-mutuel.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Groupe Mutuel crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_GROUPE_MUTUEL_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-groupe-mutuel-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Groupe Mutuel crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="groupe-mutuel"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/groupe-mutuel.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Groupe Mutuel jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Holmes Place Jobs (Dedicated)"
        id: crawler-holmes-place
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-holmes-place.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-holmes-place.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Install Playwright Chromium"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npx playwright install --with-deps chromium
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install Playwright Chromium failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Holmes Place crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_HOLMES_PLACE_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-holmes-place-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Holmes Place crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="holmes-place"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/holmes-place.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Holmes Place jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Città di Bellinzona Jobs (Dedicated)"
        id: crawler-citta-di-bellinzona
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-citta-di-bellinzona.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-citta-di-bellinzona.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Città di Bellinzona crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_BELLINZONA_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '1' }}"
            npm run jobs:update:citta-di-bellinzona
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Città di Bellinzona crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="citta-di-bellinzona"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/citta-di-bellinzona.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '1' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "🏛️ Auto-update Città di Bellinzona jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Bühler Group Jobs (Dedicated)"
        id: crawler-buehler
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-buehler.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-buehler.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Bühler Group crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_BUEHLER_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-buehler-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Bühler Group crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="buehler"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/buehler.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Bühler Group jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issue"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Belimo Jobs (Dedicated)"
        id: crawler-belimo
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-belimo.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-belimo.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Belimo crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_BELIMO_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-belimo-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Belimo crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="belimo"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/belimo.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Belimo jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update Clinique de La Source Jobs (Dedicated)"
        id: crawler-clinique-la-source
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-clinique-la-source.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-clinique-la-source.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run dedicated Clinique de La Source crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_CRAWLER_TIMEOUT_MS="${{ github.event.inputs.timeout_ms || '' }}"
            export JOBS_CRAWLER_USE_FIRESTORE_CONFIG="1"
            export JOBS_CLINIQUE_LA_SOURCE_STRICT="${{ github.event.inputs.strict_localization || '1' }}"
            export CRAWLER_SLICE_ONLY="1"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-clinique-la-source-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run dedicated Clinique de La Source crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="clinique-la-source"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/clinique-la-source.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "💼 Auto-update Clinique de La Source jobs (dedicated crawler)" data/jobs-crawler-adapters/
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: "Crawler: Update AFRY jobs"
        id: crawler-afry
        background: true
        env:
          SLUG_HISTORY_SUMMARY_FILE: /tmp/slug-history-summary-afry.txt
        run: |-
          set +e  # composite emulates per-step continue-on-error/if:failure(); no global -e
          __STEP_FAILED=0
          __GITHUB_ENV_FILE="/tmp/gh-env-afry.sh"
          : > "$__GITHUB_ENV_FILE"

          echo "::group::Install dependencies"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            npm ci --ignore-scripts
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Install dependencies failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Prepare Firebase credentials (optional)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export FIREBASE_SERVICE_ACCOUNT_JSON="${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}"
            if [ -n "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
              printf '%s' "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase-sa.json
            else
              echo "ℹ️ Firebase secrets not set — crawler will use file config only."
              exit 0
            fi
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Prepare Firebase credentials (optional) failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Load secrets from Remote Config"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
            node scripts/load-rc-env.mjs
            echo "GH_TOKEN=$GH_TOKEN" >> "$GITHUB_ENV"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Load secrets from Remote Config failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Run AFRY dedicated crawler"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            node scripts/update-afry-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Run AFRY dedicated crawler failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Housekeeping — remove expired job listings (scoped)"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export JOBS_HOUSEKEEPING_SCOPE="afry"
            export JOBS_SLICE_FILE="data/jobs/by-crawler/afry.json"
            node scripts/cleanup-jobs.mjs
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            echo "::warning::Housekeeping — remove expired job listings (scoped) failed (exit $__THIS_STEP_EXIT) — continue-on-error, not failing the crawler"
          fi
          echo "::endgroup::"

          echo "::group::Commit and push"
          (
            export GITHUB_ENV="$__GITHUB_ENV_FILE"
            export SKIP_AI_TRANSLATION="${{ github.event.inputs.skip_ai_translation || '0' }}"
            bash scripts/lib/git-commit-data.sh --slice-only "🏗️ Auto-update AFRY jobs (dedicated crawler)"
          )
          __THIS_STEP_EXIT=$?
          set -a; source "$__GITHUB_ENV_FILE"; set +a
          if [ "$__THIS_STEP_EXIT" != "0" ]; then
            __STEP_FAILED=1
            echo "::error::Commit and push failed (exit $__THIS_STEP_EXIT)"
          fi
          echo "::endgroup::"

          echo "::group::Report failure to GitHub Issues"
          if [ "$__STEP_FAILED" != "0" ]; then
            (
              export GITHUB_ENV="$__GITHUB_ENV_FILE"
              export GH_TOKEN="${{ secrets.GITHUB_TOKEN }}"
              node scripts/lib/github-issue-creator.mjs \
                --title "Crawler Failure: ${{ github.workflow }}" \
                --description "## Crawler fallito
              **Run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
              **Branch:** ${{ github.ref_name }}
              **Trigger:** ${{ github.event_name }}" \
                --priority 2 \
                --label Bug \
                --workflow "${{ github.workflow }}"
            )
          else
            echo "(skipped — no prior failure in this crawler's steps)"
          fi
          echo "::endgroup::"

          rm -f "$__GITHUB_ENV_FILE"
          if [ "$__STEP_FAILED" != "0" ]; then
            exit 1
          fi
          exit 0
      - name: Wait for all 26 crawlers in crawler-group-07
        wait-all: true
