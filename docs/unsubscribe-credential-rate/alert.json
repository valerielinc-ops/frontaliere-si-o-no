{
  "priority": 2,
  "title": "[unsub-credential] fallback_rate_warn: quota fallback ac su unsubscribe",
  "body": "Finestra: ultime 168h · canale `unsubscribe_link` · 218 eventi `unsubscribe` letti (tutti i canali) nella finestra allargata\n\n| famiglia | conteggio | nel rapporto |\n|---|---:|---|\n| email_token | 129 | denominatore |\n| autologin_code (fallback) | 36 | **numeratore** |\n| legacy_auth_token (link `at`/`authToken`) | 0 | denominatore |\n| missing (nessun campo `credential`) | 0 | no — vedi `uncredentialed_share` |\n\n**quota fallback: 21.82%** su 165 unsubscribe graduati (165 eventi `unsubscribe_link` totali nella finestra, 53 scartati come ripetizioni della stessa email+credential entro 15 minuti).\nFallback `autologin_code` osservato prima d'ora: sì.\n\n- 🔴 `fallback_rate_warn` (p2) — Quota fallback `autologin_code` 21.82% ≥ 10.00% su 165 unsubscribe graduati: da guardare prima del flip della TTL su `ac` (#5724).\n\n## Scheda\n\n**1-CAUSA.** (ipotesi, da confermare.) 21.82% degli unsubscribe graduati usa\nil fallback `autologin_code`, oppure la quota di eventi senza credential è troppo alta.\nLa misura segnala il rischio ma non distingue da sola template, token o writer difettoso.\n\n**2-FIX.** Verificare il writer o il template newsletter indicato dal runbook e correggere la\nsuperficie confermata. | **REPO**: sito; non preassegnata qui.\n\n**3-METRICA.** prima=21.82% fallback su 165 eventi graduati atteso=<10.00% e missing sotto soglia | **COMANDO**: `node scripts/check-unsubscribe-credential-rate.mjs --json --dry-run`\n\nIl comando rilegge gli eventi Firestore, valuta il finding e stampa il verdetto senza\nscrivere storia o alert.json: la scheda si chiude quando non c'è un finding alerting.\n\n**4-OSSERVATORE.** `.github/workflows/unsubscribe-credential-monitor.yml`, che legge ogni giorno il gruppo\ndi eventi e apre/chiude le issue in base ad `alert.json`. La serie è in\n`docs/unsubscribe-credential-rate/`; il comando qui sopra è la verifica read-only.\n\n**5-FALLIMENTO.** `[unsub-credential] <finding>: quota fallback ac su unsubscribe`\n\n\n### Runbook — cosa fare adesso\n\n1. **Guarda i log** di `newsletterManageSubscription` (Cloud Functions) per gli unsubscribe recenti con `credential: \"autologin_code\"`: la stessa email che riceve la newsletter ha un link footer col `token` scoped rotto?\n2. **Non è (ancora) un blocco**: `ac` non ha oggi TTL/revoca attive (i tre parametri Remote Config del rollout #5724 sono vuoti) — chi passa dal fallback esce comunque. Il numero qui dice quante persone smetterebbero di poterlo fare IL GIORNO in cui quel rollout si accende.\n3. Se la quota resta sopra soglia, verifica il template dell'email newsletter: il link `/disiscrivi-newsletter/` porta ancora un `token` scoped valido nel footer di OGNI invio (non solo quelli recenti)?\n\nSoglie: ≥ 10.00% → da guardare · ≥ 25.00% → il token primario sta fallendo su una fetta larga · baseline = 0 finché nessun `autologin_code` è mai stato osservato.\n\nFinding: fallback_rate_warn"
}
