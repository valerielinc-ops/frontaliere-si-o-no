follow-up(#5611): 1 item deferred — fix(email): sblocca i 281 iscritti soppressi da un reject
## Origine
- PR: #5611 fix(email): sblocca i 281 iscritti soppressi da un reject, senza resuscitare i 14 hard (merged 2026-08-11T09:08:33Z)
- URL: https://github.com/valerielinc-ops/frontaliere-si-o-no/pull/5611

## Item

### 1. Eseguire `--apply` per ripristinare i 281 iscritti reject-mismapped
- Source: PR body `## Non implementato`
- Original text:
  > Esecuzione di `--apply` sui 281 — `blocked: e' una decisione del proprietario`. Lo script scrive su dati di produzione e il service account ha pieni poteri; l'agente lo ha eseguito **solo in dry-run**. Il comando e' `node scripts/restore-mailtrap-suspension-suppressions.mjs --apply` (facoltativo `--limit N` per un primo lotto di prova), e i bucket sopra sono esattamente cio' che stampa prima di scrivere.
- Funnel impact: monetizzazione / traffico — 281 iscritti sono oggi bloccati da OGNI invio email (newsletter, job alert, drip onboarding, digest annunci salvati) per un'inferenza di stato che il codice attuale non produce più
- Rationale: item `blocked: decisione del proprietario` con causa esterna reale (scrittura su prod, service account); il fix è già mergiato e verificato in dry-run (281 da ripristinare, 14 hard bounce reali correttamente esclusi)
- Suggested action: proprietario esegue `node scripts/restore-mailtrap-suspension-suppressions.mjs --apply` (eventualmente con `--limit N` per un primo lotto), poi verifica i bucket stampati prima di confermare il lotto completo
