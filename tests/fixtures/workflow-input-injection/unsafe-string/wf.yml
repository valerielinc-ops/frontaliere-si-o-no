name: fixture-unsafe-string
on:
  workflow_dispatch:
    inputs:
      target:
        description: free text
        type: string
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Do the thing
        run: node scripts/thing.mjs "${{ inputs.target }}"
