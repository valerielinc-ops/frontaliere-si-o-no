# Firebase Configuration (public, safe to commit)
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=auth.frontaliereticino.ch
VITE_FIREBASE_PROJECT_ID=frontaliere-ticino
VITE_FIREBASE_STORAGE_BUCKET=frontaliere-ticino.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=957502085858
VITE_FIREBASE_APP_ID=1:957502085858:web:4941e8997ebf75b0145cbb
VITE_FIREBASE_MEASUREMENT_ID=G-LGJ9LE360F

# Amazon partner tag is hardcoded in creatorProductsService.ts (luigi066-21).
# Amazon Creators API credentials (AMAZON_CREATOR_ID, AMAZON_CREATOR_SECRET)
# are loaded from Firebase Remote Config at build time via scripts/load-rc-env.mjs.
#
# All other secrets (API keys, PATs, reCAPTCHA keys) are loaded
# exclusively from Firebase Remote Config at runtime.
# Do NOT add them here — they must never appear in source or build output.
#
# Remote Config keys expected by the app.
# Fallback defaults in code (services/firebase.ts) are currently:
# - SEO_SERP_EXPERIMENT_ENABLED = "true"
# - SEO_SERP_EXPERIMENT_VARIANT = "year_intent"
# - SEO_SERP_EXPERIMENT_TARGETS = "*"
# - SEO_SERP_EXPERIMENT_YEAR = "2026"
